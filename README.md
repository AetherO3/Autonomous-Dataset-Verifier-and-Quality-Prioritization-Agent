# Autonomous Dataset Verifier

This project is a simple system that checks a dataset, finds problems, and tries to fix them automatically.

It works step by step. First, it looks at the data and understands what each column contains. Then it checks for common issues like missing values or columns that don’t have useful information. After that, it decides what to do for each problem, like filling missing values, dropping a column, or just flagging it.
